---
phase: 01-release-feed-reliability-foundation
plan: 01
type: execute
wave: 1
depends_on: []
requirements:
  - SAFE-01
  - SAFE-03
  - SAFE-04
files_modified:
  - src/lib/release-feed/schema.ts
  - src/lib/release-feed/fetch-stable-release.ts
  - src/lib/release-feed/normalize-release.ts
  - src/lib/release-feed/allowlist.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - Stable feed payload is runtime-validated before any UI exposure.
    - Only allowlisted hosts survive download URL filtering.
    - Feed cache/revalidation behavior is explicit in implementation.
  artifacts:
    - path: src/lib/release-feed/schema.ts
      provides: zod schema and parse entrypoint for stable feed
    - path: src/lib/release-feed/fetch-stable-release.ts
      provides: fetch path with explicit timeout + revalidate behavior
    - path: src/lib/release-feed/allowlist.ts
      provides: approved-host filtering utilities
    - path: src/lib/release-feed/normalize-release.ts
      provides: UI-safe normalization contract
  key_links:
    - from: src/lib/release-feed/fetch-stable-release.ts
      to: src/lib/release-feed/schema.ts
      via: validate parsed JSON before returning model
    - from: src/lib/release-feed/normalize-release.ts
      to: src/lib/release-feed/allowlist.ts
      via: sanitize download URLs before view-model output
---

# Phase 1 Plan 01: Build release-feed reliability core

## Objective
- **What:** Implement the release-feed domain module with runtime schema validation, bounded fetch strategy, and host filtering.
- **Why:** SAFE-01/03/04 are foundational safety constraints for all later download and metadata UI.
- **Output:** A reusable server-side release-feed pipeline that returns a deterministic, UI-safe model.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-release-feed-reliability-foundation/1-CONTEXT.md
@.planning/phases/01-release-feed-reliability-foundation/01-RESEARCH.md
@.planning/phases/01-release-feed-reliability-foundation/01-VALIDATION.md

## Tasks

<task type="code">
  <name>Define stable feed schema and parser contract</name>
  <files>src/lib/release-feed/schema.ts</files>
  <action>Create strict zod schema(s) for stable release payload and export a single parser API that returns typed output or structured parse failure metadata. Include fields required by download and metadata surfaces.</action>
  <verify>pnpm test -- src/__tests__/unit/release-feed.spec.ts -t schema</verify>
  <done>Schema rejects malformed payloads and parser contract is importable by fetch pipeline.</done>
</task>

<task type="code">
  <name>Implement fetch + cache/revalidate behavior</name>
  <files>src/lib/release-feed/fetch-stable-release.ts</files>
  <action>Implement stable.json retrieval with explicit revalidation policy, timeout/abort, and failure mapping to degraded status metadata. Keep behavior server-safe and side-effect free.</action>
  <verify>pnpm test -- src/__tests__/unit/release-feed.spec.ts -t cache</verify>
  <done>Feed fetch path has deterministic cache semantics and resilient error handling with no thrown runtime leak to caller.</done>
</task>

<task type="code">
  <name>Add approved-host URL filtering and final normalization</name>
  <files>src/lib/release-feed/allowlist.ts, src/lib/release-feed/normalize-release.ts</files>
  <action>Implement allowlist enforcement for download URLs and normalize parsed payload into a UI-safe model. Drop unsafe links while preserving approved links and fallback release URL metadata.</action>
  <verify>pnpm test -- src/__tests__/unit/release-feed.spec.ts -t host</verify>
  <done>No disallowed host reaches output model and normalization emits stable ok/degraded contract.</done>
</task>

## Verification
- `pnpm lint`
- `pnpm typecheck`
- `pnpm test -- src/__tests__/unit/release-feed.spec.ts`

## Success Criteria
- [ ] SAFE-01 satisfied with runtime schema validation before UI use.
- [ ] SAFE-03 satisfied with approved-host-only links in normalized output.
- [ ] SAFE-04 satisfied with explicit cache/revalidation policy in fetch implementation.
- [ ] Core pipeline exports deterministic `ok | degraded` status contract for downstream UI.
