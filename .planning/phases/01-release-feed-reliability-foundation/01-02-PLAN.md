---
phase: 01-release-feed-reliability-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
requirements:
  - SAFE-02
  - SAFE-01
  - SAFE-03
  - SAFE-04
files_modified:
  - src/app/[locale]/page.tsx
  - src/components/landing/DownloadSection.tsx
  - messages/en.json
  - messages/zh.json
  - src/__tests__/unit/release-feed.spec.ts
  - src/__tests__/integration/landing-release-state.spec.tsx
autonomous: true
user_setup: []
must_haves:
  truths:
    - Feed failure never crashes landing page rendering.
    - Degraded state is visibly communicated in download context in both locales.
    - Unsafe link filtering and freshness behavior are represented by UI fallbacks.
  artifacts:
    - path: src/components/landing/DownloadSection.tsx
      provides: localized download rendering with degraded-state handling
    - path: messages/en.json
      provides: en fallback/degraded/freshness copy keys
    - path: messages/zh.json
      provides: zh fallback/degraded/freshness copy keys
    - path: src/__tests__/integration/landing-release-state.spec.tsx
      provides: degraded-path render coverage without page crash
  key_links:
    - from: src/app/[locale]/page.tsx
      to: src/lib/release-feed/fetch-stable-release.ts
      via: server-side data retrieval and UI model wiring
    - from: src/components/landing/DownloadSection.tsx
      to: messages/en.json
      via: next-intl translation keys for degraded/fallback labels
    - from: src/components/landing/DownloadSection.tsx
      to: messages/zh.json
      via: locale parity for degraded/fallback labels
---

# Phase 1 Plan 02: Wire degraded-state UI and validate resilience

## Objective
- **What:** Integrate release-feed reliability contract into landing UI and add test coverage for degraded and host-filtered outcomes.
- **Why:** SAFE-02 requires user-visible degraded behavior while preserving render stability; this closes the phase loop with UX-safe handling.
- **Output:** Download experience that safely consumes release data, degrades intentionally, and is covered by automated tests.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-release-feed-reliability-foundation/1-CONTEXT.md
@.planning/phases/01-release-feed-reliability-foundation/01-RESEARCH.md
@.planning/phases/01-release-feed-reliability-foundation/01-VALIDATION.md
@.planning/phases/01-release-feed-reliability-foundation/01-01-PLAN.md

## Tasks

<task type="code">
  <name>Introduce download section contract and degraded-state rendering</name>
  <files>src/components/landing/DownloadSection.tsx, src/app/[locale]/page.tsx</files>
  <action>Create or update the download section to consume the release-feed model, show degraded notice + fallback release action, and preserve stable render path on feed failures. Keep copy neutral and concise per context decisions.</action>
  <verify>pnpm test -- src/__tests__/integration/landing-release-state.spec.tsx -t degraded</verify>
  <done>Landing page remains renderable when feed fails and download context shows degraded-state message plus fallback action.</done>
</task>

<task type="code">
  <name>Add locale-safe fallback and freshness copy</name>
  <files>messages/en.json, messages/zh.json</files>
  <action>Add keys for degraded notice, notes unavailable, metadata unavailable labels, and freshness hint. Ensure key parity and consistent semantic meaning across `en` and `zh`.</action>
  <verify>pnpm test -- src/__tests__/integration/landing-release-state.spec.tsx -t locale</verify>
  <done>Both locales render complete fallback key set with no missing key runtime errors.</done>
</task>

<task type="code">
  <name>Backfill unit/integration tests for P1 reliability requirements</name>
  <files>src/__tests__/unit/release-feed.spec.ts, src/__tests__/integration/landing-release-state.spec.tsx</files>
  <action>Implement tests for schema rejection, host filtering, fetch failure degraded mapping, and non-crashing page render path. Keep assertions mapped to SAFE-01..SAFE-04.</action>
  <verify>pnpm test -- src/__tests__/unit/release-feed.spec.ts && pnpm test -- src/__tests__/integration/landing-release-state.spec.tsx</verify>
  <done>Automated coverage exercises all SAFE requirements and guards against regressions in fallback behavior.</done>
</task>

## Verification
- `pnpm lint`
- `pnpm typecheck`
- `pnpm test -- src/__tests__/unit/release-feed.spec.ts`
- `pnpm test -- src/__tests__/integration/landing-release-state.spec.tsx`

## Success Criteria
- [ ] SAFE-02 satisfied with visible degraded-state messaging and stable render fallback.
- [ ] Locale copy fallback keys for reliability states exist in both `en` and `zh`.
- [ ] Tests assert failure and host-filter scenarios without UI crash.
- [ ] Phase 1 requirements SAFE-01..SAFE-04 are covered across plan frontmatter and verification checks.
